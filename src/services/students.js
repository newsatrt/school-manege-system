import request from '../utils/request';
import config from '../utils/config';
let basePath = config.basePath;

export function fecthStudentList(values) {
  // return request(basePath + 'get-course-list', {
  //   method: 'POST',
  //   body: JSON.stringify(values)
  // });

  return {

  }
}
